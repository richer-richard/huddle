/target

# local planning notes (kept out of git)
plan.md

# personal agent working-method notes (Claude Code), not for git
CLAUDE.local.md

# local agent/session state (Claude Code, etc.)
.claude/

# local SQLite database (runtime state, not for git)
huddle-server.db
huddle-server.db-wal
huddle-server.db-shm
huddle-server.db-journal
